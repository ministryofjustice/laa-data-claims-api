# openapi-generator ignore file for the amendment generation task.
#
# The amendment spec transitively references shared schemas (claim_base,
# claim_status, fee_calculation_patch, validation_message_patch, ...). Those
# classes are already generated by the standard `openApiGenerate` task into the
# SAME package, so we must NOT write them again here (that would create duplicate
# classes on the classpath).
#
# Strategy: ignore everything, then re-include ONLY ClaimAmendmentPatch.java.
# ClaimAmendmentPatch still imports the shared types by their fully-qualified
# package name, which resolve against the standard generated sources.
**/*
!**/model/ClaimAmendmentPatch.java

