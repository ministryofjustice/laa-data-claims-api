meta {
  name: single claim
  type: http
  seq: 2
}

get {
  url: http://{{HOST}}/api/v1/claims/:claim_id/assessments
  body: none
  auth: inherit
}

params:path {
  claim_id: {{CLAIM_ID}}
}

headers {
  Authorization: {{AUTH_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
